diff --git a/build/types/props.d.ts b/build/types/props.d.ts
--- a/build/types/props.d.ts
+++ b/build/types/props.d.ts
@@ -47,6 +47,8 @@ export type PoseCameraProps = {
     /** Inference on or off. `false` releases GPU resources, the preview keeps running. */
     detection?: boolean;
     overlay?: boolean | OverlayConfig;
+    /** Covers the preview while keeping the native skeleton overlay visible. */
+    blackout?: boolean;
     data?: DataConfig;
     triggers?: readonly Trigger[];
     /** Raises the level while this camera is mounted. `setLogLevel()` sets it globally. */
diff --git a/src/types/props.ts b/src/types/props.ts
--- a/src/types/props.ts
+++ b/src/types/props.ts
@@ -64,6 +64,8 @@ export type PoseCameraProps = {
   /** Inference on or off. `false` releases GPU resources, the preview keeps running. */
   detection?: boolean;
   overlay?: boolean | OverlayConfig;
+  /** Covers the preview while keeping the native skeleton overlay visible. */
+  blackout?: boolean;
 
   data?: DataConfig;
   triggers?: readonly Trigger[];
diff --git a/src/PoseCamera.tsx b/src/PoseCamera.tsx
--- a/src/PoseCamera.tsx
+++ b/src/PoseCamera.tsx
@@ -56,7 +56,7 @@ export const PoseCamera = React.forwardRef<PoseCameraRef, PoseCameraProps>(functi
   const NativeView = getNativeView();
   const nativeRef = React.useRef<NativePoseCameraView | null>(null);
 
-  const { triggers, data, overlay, active, detection } = props;
+  const { triggers, data, overlay, active, detection, blackout } = props;
 
   // During render, before anything walks the conditions: a bad config fails at the call site
   // with a path. The validator's depth limit makes the walk below safe on a cyclic config.
@@ -297,6 +297,7 @@ export const PoseCamera = React.forwardRef<PoseCameraRef, PoseCameraProps>(functi
   return (
     <NativeView
       style={props.style}
+      blackout={blackout}
       profile={props.profile}
       facing={props.facing}
       delegate={props.delegate}
diff --git a/android/src/main/java/com/posedetection/PoseDetectionModule.kt b/android/src/main/java/com/posedetection/PoseDetectionModule.kt
index 2ceaf41..06874dc 100644
--- a/android/src/main/java/com/posedetection/PoseDetectionModule.kt
+++ b/android/src/main/java/com/posedetection/PoseDetectionModule.kt
@@ -21,7 +21,7 @@ import com.posedetection.view.parseOverlay
 import expo.modules.interfaces.permissions.PermissionsResponse
 import expo.modules.interfaces.permissions.PermissionsStatus
 import expo.modules.kotlin.functions.Queues
-import expo.modules.kotlin.jni.NativeArrayBuffer
 import expo.modules.kotlin.modules.Module
 import expo.modules.kotlin.modules.ModuleDefinition
+import java.nio.ByteBuffer
 
@@ -60,7 +60,7 @@ class PoseDetectionModule : Module() {
                         angleJoints = angleJointsFrom(options),
                         selection = selectionFrom(options),
                     )
-                }.onSuccess { promise.resolve(NativeArrayBuffer.wrap(it)) }
+                }.onSuccess { promise.resolve(it.toByteArray()) }
                     .onFailure { promise.reject("DETECTION_FAILED", it.message ?: "detection failed", null) }
             }
 
@@ -84,7 +84,7 @@ class PoseDetectionModule : Module() {
                             sendEvent("onVideoProgress", mapOf("taskId" to taskId, "progress" to progress))
                         },
                     )
-                }.onSuccess { promise.resolve(NativeArrayBuffer.wrap(it)) }
+                }.onSuccess { promise.resolve(it.toByteArray()) }
                     .onFailure { promise.reject("DETECTION_FAILED", it.message ?: "detection failed", null) }
             }
 
@@ -188,8 +188,11 @@ class PoseDetectionModule : Module() {
                 Prop("analysisResolution") { view: PoseCameraView, value: String? ->
                     view.setAnalysisResolution(value ?: "auto")
                 }
+                Prop("blackout") { view: PoseCameraView, value: Boolean? ->
+                    view.setBlackout(value ?: false)
+                }
                 // `Any?` and a cast, like `overlay`: a star-projected Map has no registered type
                 // converter, and that failure would only show up on a device.
                 Prop("data") { view: PoseCameraView, value: Any? ->
                     view.setData(parseData(value as? Map<*, *>))
                 }
@@ -318,6 +318,13 @@ class PoseDetectionModule : Module() {
         }
 }
 
+private fun ByteBuffer.toByteArray(): ByteArray {
+    val source = duplicate()
+    val bytes = ByteArray(source.remaining())
+    source.get(bytes)
+    return bytes
+}
+
 /** Resolved by JavaScript for the live path, and passed the same way here. */
 internal fun angleJointsFrom(options: Map<String, Any?>?): Array<String> {
     val raw = options?.get("angleJoints") as? List<*> ?: return Skeleton.ANGLE_JOINT_NAMES
diff --git a/android/src/main/java/com/posedetection/view/PoseCameraView.kt b/android/src/main/java/com/posedetection/view/PoseCameraView.kt
index 64a4740..07e2296 100644
--- a/android/src/main/java/com/posedetection/view/PoseCameraView.kt
+++ b/android/src/main/java/com/posedetection/view/PoseCameraView.kt
@@ -11,4 +11,5 @@ import android.os.Handler
 import android.os.Looper
 import android.os.SystemClock
 import android.util.Size
+import android.view.View
 import android.widget.FrameLayout
@@ -51,7 +51,7 @@ import com.posedetection.performance.ThermalPolicy
 import com.posedetection.performance.ThermalState
 import com.posedetection.performance.Tiers
 import expo.modules.kotlin.AppContext
-import expo.modules.kotlin.jni.NativeArrayBuffer
 import expo.modules.kotlin.viewevent.EventDispatcher
 import expo.modules.kotlin.views.ExpoView
+import java.nio.ByteBuffer
 import java.util.concurrent.Executors
@@ -80,10 +80,16 @@ class PoseCameraView(
 
     private val previewView =
         PreviewView(context).apply {
-            implementationMode = PreviewView.ImplementationMode.PERFORMANCE
+            implementationMode = PreviewView.ImplementationMode.COMPATIBLE
             scaleType = PreviewView.ScaleType.FILL_CENTER
         }
     private val overlayView = OverlayView(context)
+    private val blackoutView =
+        View(context).apply {
+            setBackgroundColor(Color.BLACK)
+            visibility = GONE
+            isClickable = false
+        }
 
     // One dedicated thread. The analyzer runs here and calls detectAsync here, so sample buffers
     // never hop threads and the UI never waits on inference.
@@ -280,3 +280,4 @@ class PoseCameraView(
     private var propAnalysis: String = "auto"
     private var overlayEnabled: Boolean = true
+    private var blackoutEnabled: Boolean = false
     private var pendingOverlayConfig: OverlayConfig = OverlayConfig()
@@ -317,13 +317,22 @@ class PoseCameraView(
         )
+        container.addView(
+            blackoutView,
+            FrameLayout.LayoutParams(
+                FrameLayout.LayoutParams.MATCH_PARENT,
+                FrameLayout.LayoutParams.MATCH_PARENT,
+            ),
+        )
         container.addView(
             overlayView,
             FrameLayout.LayoutParams(
                 FrameLayout.LayoutParams.MATCH_PARENT,
                 FrameLayout.LayoutParams.MATCH_PARENT,
             ),
         )
         addView(container)
+        blackoutView.z = 1f
+        overlayView.z = 2f
 
         // Props have not arrived yet. Without this a frame landing first would find no layout and
         // be dropped, and `snapshotFrame()` would answer empty for reasons nobody could see.
         applyFrameLayout()
@@ -426,5 +426,6 @@ class PoseCameraView(
     fun onPropsUpdated() {
         overlayView.config = pendingOverlayConfig
         overlayView.visibility = if (overlayEnabled) VISIBLE else GONE
+        blackoutView.visibility = if (blackoutEnabled) VISIBLE else GONE
 
         applyFrameLayout()
@@ -1294,3 +1294,3 @@ class PoseCameraView(
     private fun syncOverlayMirroring() {
-        overlayView.setMirrored(camera.facing == Facing.FRONT)
+        overlayView.setMirrored(camera.facing == Facing.FRONT)
     }
@@ -1322,6 +1322,11 @@ class PoseCameraView(
         overlayView.visibility = if (enabled) VISIBLE else GONE
     }
 
+    fun setBlackout(enabled: Boolean) {
+        blackoutEnabled = enabled
+        blackoutView.visibility = if (enabled) VISIBLE else GONE
+    }
+
     /**
      * Runs the precedence chain and adopts the result. [reason] is what `onPerformanceChange`
      * reports; null means this is a props update rather than something the engine decided, and
@@ -1379,13 +1379,12 @@ class PoseCameraView(
         frames.setLayout(next)
     }
 
-    fun drainFrames(): NativeArrayBuffer = NativeArrayBuffer.wrap(frames.drain())
+    fun drainFrames(): ByteArray = frames.drain().toByteArray()
 
-    fun snapshotFrame(): NativeArrayBuffer = NativeArrayBuffer.wrap(frames.snapshot())
+    fun snapshotFrame(): ByteArray = frames.snapshot().toByteArray()
 
     /** An unknown or spent ticket is an empty buffer, which is the documented contract. */
-    fun takeTriggerSnapshot(snapshotId: Int): NativeArrayBuffer =
-        NativeArrayBuffer.wrap(frames.takeSnapshot(snapshotId))
+    fun takeTriggerSnapshot(snapshotId: Int): ByteArray = frames.takeSnapshot(snapshotId).toByteArray()
 
     fun currentState(): Map<String, Any?> =
         mapOf(
@@ -1646,6 +1645,13 @@ class PoseCameraView(
     }
 }
 
+private fun ByteBuffer.toByteArray(): ByteArray {
+    val source = duplicate()
+    val bytes = ByteArray(source.remaining())
+    source.get(bytes)
+    return bytes
+}
+
 internal fun Facing.nameForJs(): String = if (this == Facing.FRONT) "front" else "back"
 
 internal fun Size.toMap(): Map<String, Any?> = mapOf("width" to width, "height" to height)
diff --git a/android/src/main/java/com/posedetection/camera/CameraSource.kt b/android/src/main/java/com/posedetection/camera/CameraSource.kt
--- a/android/src/main/java/com/posedetection/camera/CameraSource.kt
+++ b/android/src/main/java/com/posedetection/camera/CameraSource.kt
@@ -200,6 +200,8 @@ internal class CameraSource(
 
         val lens = resolveAvailable(provider, target)
         val rotation = currentRotation()
+        // Mirror the front-camera preview and overlay together so left/right landmarks stay aligned.
+        previewView.scaleX = if (lens == Facing.FRONT) -1f else 1f
 
         val preview =
             Preview
@@ -225,6 +225,7 @@ internal class CameraSource(
         provider.unbindAll()
         provider.bindToLifecycle(owner, selectorFor(lens), preview, analysis)
         preview.surfaceProvider = previewView.surfaceProvider
+        previewView.scaleX = if (lens == Facing.FRONT) -1f else 1f
 
         this.analysis = analysis
         this.facing = lens
diff --git a/android/src/main/java/com/posedetection/detector/PoseDetector.kt b/android/src/main/java/com/posedetection/detector/PoseDetector.kt
--- a/android/src/main/java/com/posedetection/detector/PoseDetector.kt
+++ b/android/src/main/java/com/posedetection/detector/PoseDetector.kt
@@ -256,6 +256,6 @@ internal class PoseDetector private constructor(
             Array(QUARTER_TURNS) { quarter ->
                 ImageProcessingOptions
                     .builder()
-                    .setRotationDegrees(-(quarter * DEGREES_PER_QUARTER))
+                    .setRotationDegrees(-(quarter * DEGREES_PER_QUARTER))
                     .build()
             }
